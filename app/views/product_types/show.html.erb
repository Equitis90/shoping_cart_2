<%= form_for @product_type do |f| %>
    <div class="row">
      <div class="col-xs-4">
        <%= f.text_field :title, value: @product_type.title %>
      </div>
      <div class="col-xs-8 text-right">
        <div class="btn-group">
          <%= f.submit "Save", class: "btn btn-primary" %>
        </div>
      </div>
    </div>
<% end %>